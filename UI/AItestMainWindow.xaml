<Window x:Class="AILayoutAgent.UI.AItestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AItest Layout Agent"
        Height="500"
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowActivated="False"
        ShowInTaskbar="False">
    <StackPanel Margin="12">
        <TextBlock Text="点击“框选”后，请回到 Revit 视图中框选构件/线。框选完成后会按当前平面名称解析的标高进行筛选；然后点击“确定”才会调用 Dify。"
                   TextWrapping="Wrap" />

        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="SelectButton" Height="34" MinWidth="140" Content="框选并筛选" Click="SelectButton_Click" />
            <Button x:Name="ConfirmButton" Margin="8,0,0,0" Height="34" MinWidth="80" Content="确定" Click="ConfirmButton_Click" IsEnabled="False" />
            <Button x:Name="CancelButton" Margin="8,0,0,0" Height="34" MinWidth="80" Content="取消" Click="CancelButton_Click" IsEnabled="False" />
            <Button x:Name="DrawButton" Margin="8,0,0,0" Height="34" MinWidth="80" Content="画线" Click="DrawButton_Click" IsEnabled="False" />
        </StackPanel>

        <TextBlock Margin="0,12,0,4" Text="Dify 输出（streaming）：" />
        <TextBox x:Name="DifyOutput"
                 Height="260"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" />

        <TextBlock x:Name="StatusText" Margin="0,12,0,0" TextWrapping="Wrap" />
    </StackPanel>
</Window>
